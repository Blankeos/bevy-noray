[package]
name = "bevy-noray"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.14"
tokio = { version = "1.0", features = ["full"] }
bincode = "1.3"
serde = { version = "1.0", features = ["derive"] }
crossbeam-channel = "0.5"

[profile.dev.package."*"]
opt-level = 3
